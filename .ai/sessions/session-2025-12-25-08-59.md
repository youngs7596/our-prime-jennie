# Session Handoff: Fix Scout Holiday Check

## 요약 (Summary)
Scout 서비스가 휴장일(예: 크리스마스)에도 실행되는 문제를 해결했습니다. 기존의 단순 로컬 시간 체크(`is_operating_hours`)를 제거하고, KIS Gateway API를 통해 정확한 장 운영 여부(`check_market_open`)를 확인하도록 로직을 변경했습니다.

## 변경 사항 (Changes)
- `services/scout-job/scout.py`:
  - `is_operating_hours` 모듈 의존성 및 호출 제거
  - `kis_api.authenticate()` 이후 `kis_api.check_market_open()` 호출 추가
- `shared/kis/gateway_client.py`:
  - `check_market_open()` 메서드 신규 구현 (`/api/market-data/check-market-open` 엔드포인트 연동)

## 다음 단계 (Next Steps)
- [ ] 다음 휴장일(예: 신정)에 Scout 서비스 로그 확인하여 정상적으로 Skip 되는지 검증
- [ ] KIS Gateway 서버 측 `/api/market-data/check-market-open` 엔드포인트 실제 구현 여부 확인 (현재는 클라이언트 코드만 수정됨)
