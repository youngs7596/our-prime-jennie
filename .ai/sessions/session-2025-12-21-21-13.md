# Session Handoff: Project Renaming & Installation Setup

## 요약 (Summary)
프로젝트 전체 브랜딩을 "Carbon Silicons Council"에서 "**my-prime-jennie**"로 변경하고, 설치 및 유지보수 편의성을 위해 데이터 저장 경로를 표준화했습니다. 또한 `install_prime.sh`의 유연성을 개선했습니다.

## 변경 사항 (Changes)
- **Project Identity**:
    - `shared/version.py`: 프로젝트 이름 및 버전을 중앙 관리.
    - `scripts/rename_project.sh`: 향후 브랜딩 변경을 위한 자동화 스크립트 작성.
    - 36개 이상의 파일에서 레거시 명칭 일괄 변경.
- **Data Persistence**:
    - `docker-compose.yml`: MariaDB 데이터 볼륨을 `/docker_data/mariadb_data`로 변경 (Host Root 경로).
    - `scripts/dump_remote_db.sh`: Docker 기반 원격 DB 덤프 도구 작성.
    - `docker/init/init_dump.sql`: 로컬 마이그레이션용 DB 덤프 생성 (gitignored).
- **Installation**:
    - `scripts/install_prime.sh`: Python 라이브러리 설치를 선택 사항(Optional)으로 변경.
    - `scripts/setup_cron_jobs.sh`: venv 경로 버그 수정.

## 다음 단계 (Next Steps)
- [ ] `scripts/install_prime.sh` 실행하여 Docker 환경 구축.
- [ ] `docker compose ps`로 모든 컨테이너(특히 MariaDB, Scheduler) 정상 가동 확인.
- [ ] 대시보드 모니터링 및 실전/모의 투자 모드 테스트.
