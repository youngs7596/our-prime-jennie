# Session Handoff: Hotfix StrategySelector & Systemd Config

## 요약 (Summary)
Scout 서비스의 크리티컬 오류(`AttributeError: StrategySelector`)를 수정하고, Systemd 서비스 파일의 문법 오류를 바로잡았습니다. 또한 주간 팩터 분석 및 일일 브리핑 Cron Job 등록을 완료했습니다.

## 변경 사항 (Changes)
- `shared/market_regime.py`: `STRATEGY_MOMENTUM`, `STRATEGY_RELATIVE_STRENGTH` 상수 복구 (Scout 호환성)
- `scripts/my-prime-jennie.service`: 환경변수 값에 공백이 포함되어 따옴표(`"`) 추가
- `scripts/setup_cron_jobs.sh`: Cron Job 등록 실행 (중복 제거 포함)

## 다음 단계 (Next Steps)
- [ ] **[필수/수동]** Systemd 서비스 파일 적용 및 재시작 (sudo 권한 필요)
  ```bash
  sudo cp scripts/my-prime-jennie.service /etc/systemd/system/
  sudo systemctl daemon-reload
  sudo systemctl restart my-prime-jennie.service
  ```
- [ ] 금요일 오후 10시 주간 팩터 분석 정상 동작 확인
