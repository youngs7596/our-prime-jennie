# Session Handoff: Fix StrategySelector, BuyExecutor & Update Cron Schedule

## 요약 (Summary)
이번 세션에서는 `scout-job`과 `buy-scanner`의 `StrategySelector` 속성 오류를 해결하고, `buy-executor`에서 발생한 `NameError` 버그를 수정했습니다. 또한, `scripts/analyze_ai_performance.py`를 Cron Job에 추가하여 일일 AI 성과 분석 자동화를 구현하고, 문서(`README.md`, `CHANGELOG.md`)를 최신화했습니다.

## 변경 사항 (Changes)
- **Bug Fixes**:
    - `scout-job`, `buy-scanner`: `StrategySelector.STRATEGY_MOMENTUM` 등 레거시 속성 인식 문제 해결 (Docker Rebuild).
    - `buy-executor`: `executor.py`의 `NameError: name 'db_conn' is not defined` 수정.
- **Infrastructure**:
    - `scripts/setup_cron_jobs.sh`: 일일 AI 성과 분석(평일 07:00) 스케줄 추가.
    - `crontab`: Clean up & re-register.
- **Documentation**:
    - `README.md`: **Analyst Module** 섹션 및 Cron 스케줄 표 추가.
    - `CHANGELOG.md`: 핫픽스 및 기능 추가 내역 업데이트.

## 다음 단계 (Next Steps)
- [ ] **모니터링**: 수정된 `buy-executor`가 실제 매수 신호를 정상 처리하는지 확인 (RabbitMQ 로그 주시).
- [ ] **모니터링**: 내일 아침 07:00 Cron Job (`analyze_ai_performance.py`) 실행 확인.
