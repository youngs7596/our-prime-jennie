# Session Handoff: Dashboard Backend Hotfix & E2E Verification

## 요약 (Summary)
이번 세션에서는 `ActivePortfolio` 마이그레이션 이후 발생한 `dashboard-backend` 서비스의 시작 실패 문제를 해결했습니다. 레거시 `Portfolio` 모델에 대한 잔존 참조를 제거하고 Docker 이미지를 리빌드하여 서비스를 정상화했습니다. 또한, Mock 환경에서의 안전성을 위해 `ACTIVE_PORTFOLIO`를 Mock 테이블 목록에 추가하고, E2E 시나리오 테스트를 통해 전체 시스템의 건전성을 검증했습니다.

## 변경 사항 (Changes)
- `services/dashboard/backend/main.py`: 삭제된 `Portfolio` 모델에 대한 `ImportError` 원인이었던 import 구문 제거.
- `shared/db/models.py`: `_MOCK_TABLES` 목록에 `ACTIVE_PORTFOLIO` 추가 (Mock 모드 데이터 격리).
- `CHANGELOG.md`: 2026-01-13 변경 이력 업데이트 (Hotfix 및 Test Safety 항목).

## 검증 결과 (Verification)
- **Dashboard Backend**: `docker compose logs` 확인 결과 에러 없이 정상 구동, DB/Redis 연결 성공.
- **E2E Tests**:
  - `test_e2e_pipeline.py`: 매수 -> DB (`ActivePortfolio`) 적재 -> 매도 시나리오 통과.
  - `test_websocket_buy_flow.py`: WebSocket 호가 -> 매수 신호 -> RabbitMQ 발행 흐름 통과.

## 다음 단계 (Next Steps)
- [ ] 대시보드 UI에서 실제 포트폴리오 데이터가 정상적으로 표시되는지 육안 확인.
- [ ] 내일 장 시작 시 `scout-job` 및 `buy-executor` 등이 `ActivePortfolio` 테이블을 사용하여 정상 거래하는지 모니터링.
