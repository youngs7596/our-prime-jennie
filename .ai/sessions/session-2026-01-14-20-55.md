# Session Handoff: System Stability & Legacy Cleanup
## 요약 (Summary)
오늘(1/14) 발생한 Buy Scanner(매수) 및 Price Monitor(매도)의 동작 중단 문제를 해결했습니다. 
Buy Scanner는 Redis 재연결 로직 부재로, Price Monitor는 좀비 쓰레드 상태로 인해 중단되었으며, 이를 모두 수정했습니다. 
또한 Airflow 도입 후 잔존하던 Legacy Scheduler Reporting 로직을 비활성화하여 불필요한 에러 로그를 제거했습니다.

## 변경 사항 (Changes)
- `services/buy-scanner/opportunity_watcher.py`: `_ensure_redis_connection` 추가 (Redis 자동 재연결)
- `services/price-monitor/main.py`: `start_monitoring` 쓰레드 종료 시 `is_monitoring` 플래그 초기화(Zombie State 방지)
- `shared/scheduler_client.py`: `mark_job_run` 함수를 No-op 처리 (Legacy Scheduler 보고 중단)
- `CHANGELOG.md`: 1/14 작업 내역 추가

## 다음 단계 (Next Steps)
- 내일 장(1/15) 정상 동작 확인 (09:00~)
- Jenkins 빌드 상태 모니터링 (병렬 빌드 제한 적용 확인)
