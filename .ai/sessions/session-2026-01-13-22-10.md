# Session Handoff: Jenkins Build Fix & Dashboard Hotfix

## 요약 (Summary)
이전 세션의 Dashboard Backend 복구 작업에 이어, Jenkins CI 파이프라인에서 간헐적으로 발생하는 Docker BuildKit 오류를 해결했습니다. 병렬 빌드 시의 캐시 경합(Race Condition)이 원인임을 파악하고 `Jenkinsfile`에 동시성 제한 설정을 추가하여 빌드 안정성을 확보했습니다.

## 변경 사항 (Changes)
- **Jenkins Build Fix**:
  - `Jenkinsfile`: `COMPOSE_PARALLEL_LIMIT = '2'` 환경변수 추가.
  - 효과: `docker compose build` 시 병렬 프로세스를 2개로 제한하여 `parent snapshot does not exist` 오류 방지.

- **Previous Hotfixes (Recap)**:
  - `services/dashboard/backend/main.py`: Legacy `Portfolio` import 제거.
  - `shared/db/models.py`: `ACTIVE_PORTFOLIO`를 `_MOCK_TABLES`에 추가 (Mock Data Isolation).
  - `CHANGELOG.md`: 2026-01-13 변경 이력 업데이트.

## 검증 결과 (Verification)
- **Jenkins Build**: 설정 변경 후 로컬 문법 검증 완료 (실제 Jenkins 트리거는 사용자 레벨에서 확인 필요).
- **Dashboard & E2E**: 앞선 세션에서 검증 완료 (Logs & Pytest pass).

## 다음 단계 (Next Steps)
- [ ] Jenkins에서 Development 브랜치 빌드 트리거하여 정상 성공 여부 확인.
- [ ] 내일 장 시작 전 시스템 전반 점검.
