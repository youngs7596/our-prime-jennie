# Session Handoff: WebSocket Approval Key Gateway 통합

## 요약 (Summary)
KIS Gateway에 WebSocket Approval Key 발급 엔드포인트를 추가하여 토큰 발급 충돌 문제를 해결했습니다. 또한 백필 스크립트의 외인순매수 이중 퍼센트 변환 버그를 수정했습니다.

## 변경 사항 (Changes)
- `services/kis-gateway/main.py`: `/api/ws-approval-key` 엔드포인트 추가 (30초 캐싱)
- `shared/kis/auth.py`: Gateway 우선 호출 + Direct API Fallback 로직 구현
- `docker-compose.yml`: buy-scanner, price-monitor에 `KIS_WS_APPROVAL_KEY_PROVIDER_URL` 환경변수 추가
- `utilities/backfill_scout_real.py`: 외인순매수 이중 `* 100` 버그 수정
- `CHANGELOG.md`: 변경 이력 업데이트

## 다음 단계 (Next Steps)
- 백필 스크립트 재실행하여 정상 외인순매수 수치 확인
- WebSocket 연결 모니터링하여 Gateway 캐싱 효과 검증
- `backfill_scout_real.py` 완료 후 E2E 백테스트 실행
